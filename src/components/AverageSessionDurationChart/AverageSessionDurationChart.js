import React from "react";

const AverageSessionDurationChart = ({ itemClass }) => {
  return <div className={itemClass}>Durée moyenne de la session</div>;
};

export default AverageSessionDurationChart;
